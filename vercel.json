{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "crons": [
    {
      "path": "/api/cron/meb-games",
      "schedule": "0 4 * * *"
    }
  ],
  "ignoreCommand": "if git log -1 --pretty=%s | grep -q '^ci: deploy approved '; then exit 1; else exit 0; fi"
}
